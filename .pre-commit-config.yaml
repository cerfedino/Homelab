repos:
  - repo: local
    hooks:
      - id: sops-check
        name: Check k3s secrets are sops-encrypted
        entry: devbox run --quiet --recompute=false -- scripts/sops-check
        language: system
        files: \.sops\.ya?ml$
        exclude: ^\.sops\.yaml$
