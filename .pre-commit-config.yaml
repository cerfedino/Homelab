repos:
  - repo: local
    hooks:
      - id: sops-check
        name: Check that every .enc file is not in cleartext
        entry: devbox run --quiet --recompute=false -- scripts/sops-check
        language: system
        files: \.enc(\.|$)
