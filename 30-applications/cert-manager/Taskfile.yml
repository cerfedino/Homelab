version: "3"

tasks:
  default:
    cmds:
      - task: apply

  apply:
    run: once
    deps: [":app-registry:apply", ":setup-helmfile-repos"]
    cmds:
      - cd .. && helmfile apply --skip-refresh -l name=cert-manager
      - cd .. && helmfile apply --skip-refresh -l name=cert-manager-issuers
