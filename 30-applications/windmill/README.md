# Windmill

- First login, change default credentials immediately
- Default instance OIDC settings are seeded by instanceSpec in the release
- SSO users arrive without a workspace and without roles so create workspace and make it default for users
