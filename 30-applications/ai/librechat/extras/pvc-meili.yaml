apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    k8up.io/backup: "false"
  name: librechat-meili-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-ephemeral
  resources:
    requests:
      storage: 2Gi
