ollama:
  models:
    pull:
      - nomic-embed-text

persistentVolume:
  enabled: true
  size: 30Gi
  storageClass: proxmox-data-zfs
  annotations:
    k8up.io/backup: "false"

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: "4"
    memory: "8Gi"
