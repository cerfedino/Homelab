ollama:
  models:
    pull:
      - nomic-embed-text

persistentVolume:
  enabled: true
  size: 30Gi
  storageClass: proxmox-data-zfs
  annotations:
    k8up.io/backup: "false"

resources:
  requests:
    memory: 1Gi
  limits:
    memory: "8Gi"
