apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: uptime-kuma-api
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ (index .Values.apps "kuma-api").internal.dns_hostname }}`)
      kind: Rule
      services:
        - name: uptime-kuma
          port: 3001
