apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hubble
spec:
  entryPoints:
    - websecure
  routes:
{{- range $variant, $v := .Values.apps.hubble }}
{{- if $v.dns_hostname }}
    - match: Host(`{{ $v.dns_hostname }}`)
      kind: Rule
      services:
        - name: hubble-ui
          port: 80
      middlewares:
        - name: authentik-forward-auth-{{ eq $variant "internal" | ternary "local" "external" }}@file
{{- end }}
{{- end }}
