apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: syncthing
spec:
  entryPoints:
    - websecure
  routes:
{{- range $variant, $v := .Values.apps.syncthing }}
{{- if $v.dns_hostname }}
    - match: Host(`{{ $v.dns_hostname }}`)
      kind: Rule
      services:
        - name: syncthing-web
          port: 8384
      middlewares:
        - name: authentik-forward-auth-{{ eq $variant "internal" | ternary "local" "external" }}@file
{{- end }}
{{- end }}
