apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-data-zfs
  resources:
    requests:
      storage: 10Gi
