- name: Setup tailscale
  hosts: tailscale_gw
  roles:
    - role: artis3n.tailscale.machine
      vars:
        tailscale_authkey: "{{ tailscale_key }}"
        tailscale_args: "--reset --advertise-routes={{ network_lan.cidr }} --accept-dns=false --advertise-exit-node"
